1、内框(叠加框)用于框定算法运算区域，一般只框定液位柱即可；
2、外框(裁剪框)用于框定图像截取区域，一般把比整个液位计稍大区域包含进去即可；
3、内框(叠加框)为红色、外框(裁剪框)为黄色，在标定状态下会默认生成这两个框，用户可以调整这两个框的大小和位置；
4、内框、外框可以拖动、可以缩放，也可以通过坐标输入框修改，程序已经限制了内框必须在外框内部，边界不重合；
5、内框的纵坐标方向像素可以指定划分为若干等分，这通过界面上的“等分数”输入框完成，最大值为20；
6、参数表中的第一个参数需与“等分数”输入框内的数值相同，这一点由用户保证；
7、内框的上边界对应的刻度值作为参数表的第二个参数，内框的下边界对应的刻度值作为参数表的第三个参数；
8、等分线对应的刻度值作为参数表第4-22共19个参数(从小到大排序)(等分线不足时，把后面的参数空着，即使填写了也无效，譬如等分数为2，那么只需填写一个等分线刻度值，即参数4，5-22号参数可以不填，也可以填入任意值)；
9、填写内框上下边界刻度值、等分线刻度值的时候可以通过选择“区域放大”为“ON”，然后拖动一个小框放大刻度位置以方便读取刻度并输入；

请根据以上要求设计用例测试，发现问题及时跟帖