#!/usr/bin/env python
# coding=utf-8

'''
aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
1、简介
	对象是Python对数据的抽象。Python程序中的所有数据都由对象或对象之间的关系表示（代码
也由对象表示）。

	每个对象由ID、类型以及值组成。

	对象一旦建立，它的ID永远不会改变，你可以认为它是该对象在内存中的地址。id(variable)
函数返回一个表示变量所引用的对象的ID。variable1 is variable2(is operator)比较变量1和
变量2是否引用同一个对象。

	对象的类型决定对象支持的操作，还决定该类型的对象可能具有的值。type()函数返回对象的
类型（它本身也是一个对象）。与ID一样，对象的类型也是不可以修改的。

	某些对象的值可能会更改。值可以改变的对象称为可变的；一旦建立，值就不可以改变的对象
称为不可变的。一个对象的可变性由它的类型决定；例如，数值、字符串和元组是不可变的，而字典
和列表是可变的。
'''

'''
aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
2、对象类型
'''