dpkg -l 列出系统中安装的所有包的状态,`ii’ 开头的是正常安装的包,`rc’开头的则是删除但仍留下配置文件的包,其他状态则是有错误的状态,自己执行一次...
2.grep “^rc” 提取以 rc 开头的包,也就是被删除但仍残留配置文件的包的...
3.awk ‘{print $2}’ 打印这些包的名字,他们位于 dpkg -l ...
4.xargs aptitude -y purge 把上述输出,也就是要清除配置文件...


dpkg -l |grep ^rc|awk '{print $2}' | xargs dpkg -P 